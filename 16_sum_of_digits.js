const getSumOfDigits = () =>{
    // let num = 1
    // let arr = []

    // while(num < Math.pow(2, 1000)){
    //     num *= 2
    //     arr.push(num.toString().split("").map(a => parseInt(a)).reduce((a, b)=> a + b, 0))
    // }
    // console.log(arr)
    console.log(Math.pow(2, 100))
}
getSumOfDigits()